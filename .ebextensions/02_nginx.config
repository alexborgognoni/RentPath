files:
  "/etc/nginx/conf.d/laravel_rewrites.conf":
    content: |
      location / {
          try_files $uri $uri/ /index.php?$query_string;
      }

container_commands:
  01_reload_nginx:
    command: "sudo service nginx reload"
